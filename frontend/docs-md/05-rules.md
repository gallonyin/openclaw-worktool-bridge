# 规则配置指南

## 匹配模式说明

- 全部匹配：不限制
- 精准匹配：完全相同
- 模糊匹配：支持更灵活匹配（可理解为“包含”）

## 推荐配置顺序

1. 先做一个“全部匹配”保底规则
2. 再加“精准匹配”覆盖重点群/人
3. 最后加“模糊匹配”处理边界场景

## 优先级建议

- 明确业务场景的规则，优先级更高
- 宽泛兜底规则，优先级更低

## 新手模板

- 私聊：全部匹配 + 默认 AI 引擎
- 群聊：仅在 @ 时回复 + 指定群名精准匹配

## 防误触建议

- 对高噪声群设置更严格匹配条件
- 先小范围灰度，再全量开启
